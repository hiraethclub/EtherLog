PyQt5>=5.15.0
platformdirs>=3.0.0
# keyring is optional but strongly recommended for secure SMTP password storage.
# Without it the app still runs and offers to store the password in the config
# file instead (with explicit user consent).
keyring>=24.0.0
